# Diário de Desenvolvimento — 19/05/2026

## O que foi feito hoje

### Mapa interativo com drill-down (Brasil → Estado → Município)
- Substituído Folium por **Plotly `go.Choropleth`** — renderização 100% no browser, zero risco de travamento
- GeoJSON dos colaboradores simplificado com `shapely` (tolerância 0.008°): **128 MB → 2 MB** (97% de redução)
- Script `scripts/preparar_geo_cache.py` criado para processar os GeoJSONs dos colaboradores
- Cache com `@st.cache_resource` — GeoJSON carregado uma única vez, compartilhado entre todas as sessões
- Navegação por clique implementada via `st.session_state` (`mapa_nivel` / `mapa_uf`)
- Botão "← Brasil" para voltar ao mapa nacional
- Função `_bbox()` adicionada para calcular bounding box real do GeoJSON (substituiu `fitbounds="locations"` que não funcionava para mapas subnacionais)

### Banco de dados real
- Identificado schema correto: `public.sinan_tube` (não `silver.sinan_tube`)
- Corrigido tipo na query: `ano_notificacao = '2025'` (coluna é `text`, não `integer`)
- Script `scripts/checar_banco.py` criado para diagnóstico de permissões
- **Conclusão:** acesso ao banco não foi possível do computador pessoal — resolver amanhã na rede da instituição ou pedir para colega exportar

### Feedback da colaboradora Raquel (incorporado ao backlog)
Recebido documento com 6 melhorias prioritárias — salvo na memória do projeto.

### Correções técnicas
- Removidos null bytes de `src/mapa_interativo.py` e `app.py`
- Sintaxe verificada em todos os arquivos modificados

---

## Pendências para amanhã (20/05/2026)

### 🔴 CRÍTICO — Mapa de municípios
O mapa do drill-down por estado exibe os polígonos municipais muito pequenos ou invisíveis.
- **Causa identificada:** `fitbounds="locations"` não funciona corretamente para mapas subnacionais no Plotly
- **Correção aplicada hoje:** função `_bbox()` que calcula `lataxis_range` / `lonaxis_range` diretamente do GeoJSON
- **Status:** código corrigido mas **não validado visualmente** — precisa testar no browser amanhã
- **Teste:** clicar em qualquer estado e confirmar que os municípios aparecem em tamanho correto

### 🔴 CRÍTICO — Banco de dados real
- Conectar na rede da instituição ou pedir a um colega para exportar `sinan_tube_2025_tratado.parquet`
- Comando na máquina com acesso:
  ```
  python scripts/conectar_banco.py 2025
  python scripts/preparar_dados.py 2025
  ```

### 🟡 IMPORTANTE — Implementar feedback da Raquel
1. Padronizar "por 100 mil habitantes" em todo o dashboard
2. Reordenar KPIs: Incidência → Mortalidade → Óbitos TB → Coinfecção HIV / Cura → Abandono → Total → Municípios prioritários
3. Especificar contexto nos títulos ("Média de incidência por mês")
4. Série temporal com os 10 indicadores epidemiológicos
5. Duas pirâmides etárias: casos e óbitos por faixa etária e sexo
6. Gráfico desfecho × status HIV (cura, abandono, óbito TB, óbito outras causas, transferência, falência, TB-DR)

### 🟢 DESEJÁVEL
- KPIs no topo do dashboard
- Validar todos os gráficos com dados reais após import do banco

---

## Apresentação: segunda-feira 25/05/2026
Restam **4 dias úteis**.
